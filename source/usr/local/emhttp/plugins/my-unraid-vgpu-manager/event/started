#!/bin/bash
# Runtime restoration is completed by event/starting before VM autostart.
# Also inspect a pending OS upgrade when the array starts.
CHECK=/usr/local/emhttp/plugins/my-unraid-vgpu-manager/include/upgrade-check.sh
nohup "$CHECK" auto >>/var/log/my-unraid-vgpu-manager-upgrade.log 2>&1 &
